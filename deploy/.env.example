# Environment Variables for ALE OmniSwitch MCP Server
#
# Copy this file to .env and fill in your values
# cp .env.example .env

# ===== AUTHENTICATION STRATEGY =====
# The server supports zone-based credential fallback:
# 1. Global credentials (tried first)
# 2. Zone-specific credentials (fallback based on IP subnet)
#
# Zone identification: 10.X.0.0/16 → Zone X (e.g., 10.9.0.1 → Zone 9)

# Global Credentials (Priority 1 - REQUIRED)
AOS_GLOBAL_USERNAME=switch_admin
AOS_GLOBAL_PASSWORD=global_secure_password

# Zone-Specific Credentials (Optional Fallback)
# Define credentials for specific network zones (10.X.0.0/16)
# Example: Zone 9 covers 10.9.0.0/16
AOS_ZONE9_USERNAME=zone9_admin
AOS_ZONE9_PASSWORD=zone9_password

# Example: Zone 1 covers 10.1.0.0/16
AOS_ZONE1_USERNAME=zone1_admin
AOS_ZONE1_PASSWORD=zone1_password

# Add more zones as needed:
# AOS_ZONE2_USERNAME=zone2_admin
# AOS_ZONE2_PASSWORD=zone2_password

# Optional: Jump Host Credentials
# AOS_JUMP_USERNAME=your_jump_username
# AOS_JUMP_PASSWORD=your_jump_password

# Optional: API Authentication
# AOS_INTERNAL_API_KEY=change-me-to-random-secure-key

# Optional: Logging
# AOS_LOG_LEVEL=INFO
